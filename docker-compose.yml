version: '3.8'

services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: hinthint_postgres
    environment:
      POSTGRES_DB: hinthint
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d hinthint"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
